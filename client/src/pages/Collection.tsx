import { ArrowUpRight } from "lucide-react";
import { Link } from "wouter";
import { collectionItems } from "@/lib/site";
import { AmbientOrb, ImageCaption, PageHero, Reveal, SafeImage, SectionLabel } from "@/components/Primitives";

export default function Collection() {
  return <div className="page collection-page"><PageHero eyebrow="Salon Oval / 03" title={<><span>The Oval</span><em>collection.</em></>} intro="A visual vocabulary for the next version of you — classic, modern, signature, bold, elegant, textured." image={collectionItems[2].image} align="right" /><section className="collection-board section-cream"><div className="container"><SectionLabel index="01">Six style directions</SectionLabel><div className="collection-board-intro"><p>These are not finished promises or client results. They are editorial starting points — a moodboard for the conversation.</p><span>Tap a frame to book an enquiry ↗</span></div><div className="collection-board-grid">{collectionItems.map((item, index) => <Link href="/appointment" className={`board-card board-${index + 1}`} key={item.number}><div className="board-image"><SafeImage src={item.image} alt={`${item.name} editorial placeholder`} /><div className="board-ring" /></div><div className="board-caption"><span>{item.number}</span><div><strong>{item.name}</strong><small>{item.description}</small></div><ArrowUpRight size={17} /></div></Link>)}</div></div></section><section className="collection-statement scene-dark"><AmbientOrb className="orb-right" /><div className="container collection-statement-inner"><span className="eyebrow">The Oval / a point of view</span><h2>Shape is a<br /><em>feeling.</em></h2><p>Bring a reference, a feeling, or simply a day you want to look like. The salon conversation begins there.</p><Link href="/appointment" className="final-cta">Bring your idea <ArrowUpRight size={18} /></Link></div></section></div>;
}
