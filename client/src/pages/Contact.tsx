import { ArrowUpRight, MapPin, Phone } from "lucide-react";
import { Link } from "wouter";
import { business } from "@/lib/site";
import { AmbientOrb, MagneticLink, PageHero, SectionLabel } from "@/components/Primitives";

export default function Contact() {
  return <div className="page contact-page"><PageHero eyebrow="Salon Oval / 07" title={<><span>Find Salon</span><em>Oval.</em></>} intro="A modern salon in Bandaragama, Sri Lanka. Call, get directions, or send an appointment enquiry to start the conversation." /><section className="contact-location scene-dark"><AmbientOrb className="orb-right" /><div className="container contact-location-grid"><div className="contact-map"><div className="map-grid" /><div className="map-road road-one" /><div className="map-road road-two" /><div className="map-road road-three" /><div className="map-pin"><MapPin size={18} /><span>Salon Oval</span></div><a href={business.mapUrl} target="_blank" rel="noreferrer" className="map-open">Open in Google Maps <ArrowUpRight size={15} /></a></div><div className="contact-card"><SectionLabel index="01">The location</SectionLabel><h2>On the DFCC<br /><em>Bank.</em></h2><address>{business.address.map((line) => <span key={line}>{line}</span>)}</address><div className="contact-lines"><a href={`tel:${business.phone.replaceAll(" ", "")}`}><Phone size={16} />{business.phone}</a><a href={business.mapUrl} target="_blank" rel="noreferrer"><MapPin size={16} />Google Business Profile <ArrowUpRight size={15} /></a></div><div className="contact-actions"><a href={`tel:${business.phone.replaceAll(" ", "")}`} className="oval-button inverted">Call now <Phone size={15} /></a><a href={business.mapUrl} target="_blank" rel="noreferrer" className="text-link light">Get directions <ArrowUpRight size={15} /></a></div></div></div></section><section className="contact-footer-cta section-cream"><div className="container"><p className="eyebrow">A look is a beginning.</p><h2>Make the<br /><em>appointment.</em></h2><MagneticLink href="/appointment">Request an appointment</MagneticLink></div></section></div>;
}
